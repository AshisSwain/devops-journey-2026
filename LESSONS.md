1.Recovered a locked 24LTS Ubuntu VM using GRUB (Reset the passward).
2.Diagnosed missing Docker volumes and missing compose files.
    commands used - sudo docker ps (for check docker containers)
                  - sudo docker volume ls (to check the docker volumes)
                  -sudo docker compose up -d
                  -sudo docker compose down
                  -sudo docker compose up -d
3.Rebuilt the entire n8n + PostgreSQL stack from scratch
4.Got n8n running and accessible in my browser.But i will setup it HTTPS server.
5.Made a git repo , Commited and pushed it.
